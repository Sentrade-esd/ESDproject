import React, { useState } from 'react';
import Row from "react-bootstrap/Row"
import Col from "react-bootstrap/Col"

function Trade(){
    return(
        <div>
            <h1>Not sure how we want to build this page</h1>
        </div>
    )
}

export default Trade;